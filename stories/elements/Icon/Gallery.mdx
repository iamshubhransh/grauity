import { IconItem, Meta } from '@storybook/addon-docs';
import orderBy from 'lodash/orderBy';
import startCase from 'lodash/startCase';

import { Icon } from 'ui/elements/Icon';
import { TAG_ICONS } from 'ui/core';
import TokenBlock from '../../helper-components/TokenBlock';


<Meta title="Elements/Icon/Gallery"/>

# Iconography

<div>
  {orderBy(Object.keys(TAG_ICONS)).map((key) => {
    return (
        <div key={key}>
            <h2>{startCase(key)}</h2>
            {
                orderBy(TAG_ICONS[key]).map((icon) => {
                    return (
                        <div key={`${key}-${icon}`}>
                            <IconItem name={<TokenBlock copy>{icon}</TokenBlock>}>
                                <Icon name={icon} color="inherit" />
                            </IconItem>
                        </div>
                    )
                })
            }
        </div>
    );
  })}
</div>
